{
  "name": "promptforge",
  "version": "1.0.0",
  "description": "PromptForge — lazy input → precise output. AI prompt expansion tool.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
